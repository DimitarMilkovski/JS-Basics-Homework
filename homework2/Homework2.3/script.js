
function parameterType(parameter){
    console.log(typeof(parameter));
}

let objectParameter = ["Dimitar","Milkovski"];
let booleanParametar = true;
let numberParameter = 5;
let stringParameter = "Dimitar";
let undefinedParameter;


parameterType(objectParameter);
parameterType(booleanParametar);
parameterType(numberParameter);
parameterType(stringParameter);
parameterType(undefinedParameter);

